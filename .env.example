# Copy this file to .env and fill in real values.
# start-local.sh and start-server.sh source .env automatically.

# =============================================================================
# InfluxDB connection (must match docker-compose.yml)
# =============================================================================
INFLUXDB_URL=http://localhost:8086
INFLUXDB_TOKEN=your-influxdb-token-here
INFLUXDB_ORG=appd
INFLUXDB_BUCKET=immune_system

# =============================================================================
# Server API store (used by start-server.sh / ApiStore mode)
# Set SERVER_API_BASE_URL to switch from direct InfluxDB to the server API.
# =============================================================================
# SERVER_API_BASE_URL=http://localhost:5000
# SERVER_API_KEY=
# SERVER_RUN_ID=
# SERVER_PORT=5000

# =============================================================================
# OpenTelemetry (optional — metrics export to OTEL collector)
# =============================================================================
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
# OTEL_SERVICE_NAME=ai-agent-immune-system
# OTEL_METRIC_EXPORT_INTERVAL_MS=5000

# =============================================================================
# LLM Gateway (passive agent observation — used by start-gateway.sh)
# =============================================================================
# LLM_UPSTREAM_URL=https://api.openai.com
# GATEWAY_PORT=4000
# GATEWAY_POLICIES=[]

# MCP Proxy (optional — tool-level observation)
# MCP_UPSTREAM_URL=http://localhost:3000
# MCP_PROXY_PORT=4001

# =============================================================================
# Immune system runtime
# =============================================================================
# RUN_DURATION_SECONDS=1200

# Ingest API key for the web dashboard's HTTP ingest endpoint.
# Auto-generated and cached if not set — set explicitly in production.
# INGEST_API_KEY=

# =============================================================================
# Cache (local state persistence across restarts)
# =============================================================================
# Directory for the local JSON cache file (run_id, baselines, quarantine, API key).
# Defaults to ~/.immune_cache. Set this in Docker/K8s to a mounted volume.
# IMMUNE_CACHE_DIR=/data/immune-cache

# =============================================================================
# Logging
# =============================================================================
# LOG_LEVEL: DEBUG, INFO, WARNING, ERROR. Default: INFO
# LOG_LEVEL=INFO

# LOG_FORMAT: "text" (colored console) or "json" (structured, for log aggregation).
# Default: text
# LOG_FORMAT=text

# FORCE_COLOR: Set to "1" to force colored output even when stdout is not a TTY.
# FORCE_COLOR=0
