# Copy this file to .env and fill in real values.
# start.sh sources .env automatically.

# InfluxDB connection (must match observability/docker-compose.yml)
INFLUXDB_URL=http://localhost:8086
INFLUXDB_TOKEN=your-influxdb-token-here
INFLUXDB_ORG=appd
INFLUXDB_BUCKET=immune_system

# OTEL collector (optional)
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
OTEL_SERVICE_NAME=ai-agent-immune-system

# Run duration (seconds). Default: 1200 for main.py
# RUN_DURATION_SECONDS=1200

# Ingest API key (optional — auto-generated and cached if not set)
# INGEST_API_KEY=

# Cache directory (optional — defaults to ~/.immune_cache).
# Set this when running in Docker/K8s to point at a mounted volume.
# IMMUNE_CACHE_DIR=/data/immune-cache

# Server API store (alternative to direct InfluxDB — set this to use ApiStore)
# SERVER_API_BASE_URL=http://localhost:5000
# SERVER_API_KEY=
# SERVER_RUN_ID=
